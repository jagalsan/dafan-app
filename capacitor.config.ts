import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'dafan.app',
    appName: 'dafan',
    webDir: 'www',
    server: {
        androidScheme: 'https',
    },
};

export default config;
